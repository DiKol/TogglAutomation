<UserControl
    x:Class="CollectionViewMVVM.Views.EmployeeListingView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CollectionViewMVVM.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="25" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"></ColumnDefinition>
                <ColumnDefinition Width="50"></ColumnDefinition>
                <ColumnDefinition Width="100"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Border Background="LightBlue" 
        BorderBrush="Black" 
        BorderThickness="1" 
        CornerRadius="1" 
        Padding="1">
            <Label x:Name="ProjectNameLabel" Content="No Project" Grid.Column="0" VerticalAlignment="Center"></Label>
            </Border>
            <Button Content="Start" Grid.Column="1" x:Name="StartStopButton" VerticalAlignment="Center"/>
            <Label x:Name="ProjectNameLabel2" Content="No Project" Grid.Column="2" VerticalAlignment="Center"></Label>
                
       </Grid>
        
        
        <TextBox Grid.Row="1" Text="{Binding EmployeesFilter, UpdateSourceTrigger=PropertyChanged}" />

        <Border
            Grid.Row="2"
            Margin="0 10 0 0"
            Padding="5"
            BorderBrush="Gray"
            BorderThickness="1"
            CornerRadius="5">
            <ListView
                BorderThickness="0"
                ItemsSource="{Binding EmployeesCollectionView}"
                >
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <EventSetter Event="PreviewMouseLeftButtonUp" Handler="ListViewItem_PreviewMouseLeftButtonDown" />
                    </Style>
                </ListView.ItemContainerStyle>
                
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="{Binding Name}" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</UserControl>
